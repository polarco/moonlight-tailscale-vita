diff --git a/libgamestream/http.c b/libgamestream/http.c
index a4ffece..d85fb39 100644
--- a/libgamestream/http.c
+++ b/libgamestream/http.c
@@ -23,6 +23,7 @@
 #include <stdbool.h>
 #include <string.h>
 #include <curl/curl.h>
+#include "tunnel.h"

 static CURL *curl;

@@ -88,7 +89,17 @@ int http_request(char* url, PHTTP_DATA data) {

     data->size = 0;
   }
+  char safe_url[192];
+  const char *query = strchr(url, '?');
+  size_t safe_length = query != NULL ? (size_t)(query - url) : strlen(url);
+  if (safe_length >= sizeof(safe_url)) safe_length = sizeof(safe_url) - 1;
+  memcpy(safe_url, url, safe_length);
+  safe_url[safe_length] = '\0';
+  tsvita_trace("HTTP", "request %s", safe_url);
+
   CURLcode res = curl_easy_perform(curl);
+  tsvita_trace("HTTP", "curl=%d (%s), response_bytes=%u", (int)res,
+               curl_easy_strerror(res), (unsigned int)data->size);

   if(res != CURLE_OK) {
     gs_error = curl_easy_strerror(res);
diff --git a/src/gui/ui_connect.c b/src/gui/ui_connect.c
index 4e30f0d..389e9c9 100644
--- a/src/gui/ui_connect.c
+++ b/src/gui/ui_connect.c
@@ -13,6 +13,7 @@
 #include "../util.h"
 #include "../device.h"
 #include "../debug.h"
+#include "tunnel.h"

 #include "client.h"
 #include "errors.h"
@@ -276,7 +277,8 @@ int ui_connect(char *name, char *address, uint16_t port) {
       display_error("Gamestream error: %s\n", gs_error);
       return 0;
     } else if (ret != GS_OK) {
-      display_error("Can't connect to server\n%s", address);
+      display_error("Can't connect to server\n%s\nret=%d: %s", address, ret,
+                    gs_error != NULL ? gs_error : "sem detalhe");
       return 0;
     }

@@ -416,7 +418,8 @@ device_info_t* ui_connect_and_pairing(device_info_t *info) {
     display_error("Gamestream error: %s\n", gs_error);
     return NULL;
   } else if (ret != GS_OK) {
-    display_error("Can't connect to server\n%s", info->internal);
+    display_error("Can't connect to server\n%s\nret=%d: %s", info->internal,
+                  ret, gs_error != NULL ? gs_error : "sem detalhe");
     return NULL;
   }

@@ -512,7 +515,11 @@ bool check_connection(const char *name, char *addr, uint16_t port) {
   char key_dir[4096];
   sprintf(key_dir, "%s/%s", config.key_dir, name);

-  if (gs_init(&server, addr, port, key_dir, log_level, true) != GS_OK) {
+  int result = gs_init(&server, addr, port, key_dir, log_level, true);
+  if (result != GS_OK) {
+    tsvita_trace("MOONLIGHT", "check_connection %s:%u ret=%d error=%s",
+                 addr != NULL ? addr : "(null)", (unsigned int)port, result,
+                 gs_error != NULL ? gs_error : "sem detalhe");
     return false;
   }

@@ -529,17 +536,23 @@ void ui_connect_paired_device(device_info_t *info) {
   }

   char *addr = NULL;
-  if (info->prefer_external && check_connection(info->name, info->external, info->port)) {
+  bool has_internal = info->internal[0] != '\0';
+  bool has_external = info->external[0] != '\0';
+  if (info->prefer_external && has_external &&
+      check_connection(info->name, info->external, info->port)) {
     addr = info->external;
-  } else if (check_connection(info->name, info->internal, info->port)) {
+  } else if (has_internal &&
+             check_connection(info->name, info->internal, info->port)) {
     addr = info->internal;
-  } else if (check_connection(info->name, info->external, info->port)) {
+  } else if (has_external &&
+             check_connection(info->name, info->external, info->port)) {
     addr = info->external;
   }
   info->prefer_external = addr == info->external;
   save_device_info(info);

   if (addr == NULL) {
-    display_error("Can't connect to server\n%s", info->name);
+    display_error("Can't connect to server\n%s\n%s", info->name,
+                  gs_error != NULL ? gs_error : "sem detalhe");
     return;
   }
diff --git a/src/connection.c b/src/connection.c
index 2906dfb..eb65a4a 100644
--- a/src/connection.c
+++ b/src/connection.c
@@ -26,6 +26,7 @@
 #include <stdbool.h>
 #include "connection_overlay.h"
 #include "debug.h"
+#include "tunnel.h"
 
 static int connection_status = LI_DISCONNECTED;
 
@@ -65,6 +66,7 @@ void connection_connection_started() {
   vita_debug_log("connection started\n");
   connection_status = LI_CONNECTED;
   start_output();
+  tsvita_session_begin();
   vitavideo_hide_poor_net_indicator();
 }
 
@@ -97,6 +99,7 @@ static void connection_connection_terminated(int error_code) {
   }
 
   LiStopConnection();
+  tsvita_session_end();
 
   if (connection_status == LI_CONNECTED) {
     stop_output();
